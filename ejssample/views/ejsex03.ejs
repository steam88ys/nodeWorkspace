<p> 예제로 만든 템플릿 입니다.</p>
<p> 데이터를 반환하여 목록으로 표시합니다.</p>
<p>
    <ol>
        <% dataArr.forEach((item, index) => { %>
            <li><%= item.name %> <%= item.age %></li>
        <% }) %>
    </ol>
</p>